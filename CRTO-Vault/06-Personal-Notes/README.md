# Personal Notes

Your own gotchas, mistakes, "why did this fail" reflections, and lessons that don't fit neatly into a technique note.

These are the insights that come from hands-on experience — the stuff the course doesn't tell you. Examples:

- "I wasted 30 minutes because I forgot to run SharpHound from an elevated context"
- "The lab DC doesn't respond to LDAP on the default port — use LDAPS"
- "Always check if CLM is enabled before running PowerShell payloads"
